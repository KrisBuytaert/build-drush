
# Build in Exit codes .. 

# Prereqs
#yum install rpm-build
#gem install fpm 
#fpm -s gem -t rpm fpm 
# GET it from git .. not from http ...  
# 
#wget http://ftp.drupal.org/files/projects/drush-7.x-4.5.tar.gz
# mkdir -p /usr/local/drush 
# cd /usr/local/ 
#rm -rf drush
#tar -vzxf drush-7.x-4.5.tar.gz 



fpm -s pear -t rpm -a all --pear-channel pear.drush.org drush


fpm -s pear -t deb -a all --pear-channel pear.drush.org drush


